[pytest]
# When running all weeks from repo root, multiple weeks reuse the same test file basenames
# (e.g. test_notes.py). Importlib mode prevents "import file mismatch" collection errors.
addopts = --import-mode=importlib
